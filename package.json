{
  "name": "prestige-motors-marketplace",
  "private": true,
  "version": "1.0.0",
  "description": "Luxury car marketplace with financing, verification, delivery, and dashboard flows.",
  "scripts": {
    "client": "npm --prefix client run dev",
    "server": "npm --prefix server run dev",
    "build": "npm --prefix client run build",
    "lint": "npm --prefix client run lint",
    "validate:catalog-media": "node scripts/validate-catalog-media.js"
  }
}
